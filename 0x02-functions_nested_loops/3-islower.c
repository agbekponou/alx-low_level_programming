#include "main.h"
/**
 * _islower - prints 1 if lower case o otherwise
 *
 * Return: return 1 if lower case else 0
 *
 *@c: The character to be checked
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
return (0);
}

